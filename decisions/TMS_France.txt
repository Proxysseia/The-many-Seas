political_decisions = {
	secure_iberian_coast = {
	picture = annals_of_the_fulani_jihad
	potential = {
			tag = FRA
			NOT = { has_country_flag = coast_secured }
		}
		allow = {
			owns = 460
			war = no
		}
		effect = {
			FRA_455 = { add_core = THIS }
			set_country_flag = coast_secured
		}
		
	}
	annex_catalonia = {
		picture = niederwald_denkmal
		potential = {
			tag = FRA
			NOT = { has_country_flag = catalan_accepted }
		}
		allow = {
			nationalism_n_imperialism = 1
			war = no
			CAT = {
				all_core = {
					OR = {
						owned_by = THIS
						owner = {
							in_sphere = THIS
						}
					}
				}
			}
		}
		effect = {
			add_accepted_culture = catalan
			prestige = 20
			set_country_flag = catalan_accepted
			}
	}
}